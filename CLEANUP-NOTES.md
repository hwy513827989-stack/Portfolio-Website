# Phase 1 cleanup

This package changes only the code structure. Existing image and document paths remain unchanged so the current visual design is not disrupted.

Changes:

- Renamed the uploaded code files to their real website filenames.
- Consolidated page-specific CSS into the bottom of `style.css`.
- Consolidated repeated JavaScript into `script.js`.
- Replaced the stale `resume.html` link with `about.html`.
- Replaced the stale Snow Mountain Open Graph URL ending in `project1.html`.
- Added `README.md` and `.gitignore`.

Not changed yet:

- `P1`, `P2`, and `P3` asset folders.
- Image filenames and extensions.
- Resume and favicon locations.
- Project order or page copy.
- The Heist page.
